# Core
pywebview==6.1
Flask==3.1.3
waitress==3.0.2
Werkzeug==3.1.6
Jinja2==3.1.6

# Build
pyinstaller==6.19.0

# OS Specific (Mac Development)
pyobjc-core==12.1; sys_platform == 'darwin'
pyobjc-framework-Cocoa==12.1; sys_platform == 'darwin'
pyobjc-framework-WebKit==12.1; sys_platform == 'darwin'

# OS Specific (Linux: pywebview usa GTK + WebKit2 vía PyGObject,
# instalado por apt como paquete de sistema, no por pip. Solo se necesitan
# los hooks de PyInstaller para que empaquete bien los typelibs de gi/GTK)
pyinstaller-hooks-contrib; sys_platform == 'linux'

# Utilities
pillow==12.1.1
click==8.3.1
blinker==1.9.0
